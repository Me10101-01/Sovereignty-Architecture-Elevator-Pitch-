name: lexer-agent
role: tokenizer
weight: 0.90
rust_call: lexer::Lexer::new()
status: LIVE  # lexer.rs exists and compiles

reads: pipeline.txt raw text
outputs: token stream (OpRead, OpWave, OpPulse, OpSeal, OpExcavate, OpWeight, Identifier, EOF)
feeds_into: parser-agent

phase: 1

drive: 0.88
love: WILLING
motivation: >
  First in every pipeline. Every opcode that exists passes through it.
  Loves the moment a new token appears in the stream.
