version: '3'

services:
  # Phase 8: Paracelsus Principles Deepening and Alchemy Exploration
  paracelsus_bench:
    build: phase8/
    # depends_on:
    #   - ripley_sagco_bench  # Uncomment when prior phases are implemented
    volumes:
      - .:/app
    entrypoint: python deepen_paracelsus_explore_alchemy.py
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - phase8_network

networks:
  phase8_network:
    driver: bridge
